dataset:
  processed_dir: "data/processed/sod"
  imgsz: 1024                              # Increased for coarse tiling (Action B) to preserve tiny defects

# Neural Network Configurations
training:
  backbone: "yolo26n-sem.pt"              # Pure Semantic Segmentation variant (STAL enabled)
  epochs: 100
  batch_size: 8                            # Reduced from 32 to prevent RTX 4090 OOM at 1024 imgsz
  lr0: 0.001
  weight_decay: 0.0005

# Industrial Double-Threshold Gating Parameters
gating_thresholds:
  pixel_thresh: 0.70                     # Individual pixel activation binarization cut-off (tau_pixel)
  anomaly_thresh: 0.0005
  near_miss_margin: 0.20                 # Log frames within 20% of anomaly_thresh for manual review
