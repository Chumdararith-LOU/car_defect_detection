# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp

# -----------------------------------------------------------------
# MLOps Data & Artifact Storage (Content-level ignore only)
# -----------------------------------------------------------------
data/raw/*
data/processed/*
data/calibration/*
data/external/*
artifacts/*

# EXCLUSIONS: Force Git to always step inside and see DVC configuration files
!**/*.dvc
!**/dvc.yaml
!**/dvc.lock
!**/.gitignore
!**/.gitkeep

# MLflow
mlruns/

# Notebooks output
notebooks/*.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

minio_data/
mlflow.db
_build/

runs/

src/Diagnost/
src/stage2/data/EDA_spliting.ipynb

AGENTS.md
.opencode

# Stage 3 local simulation artifacts
data/processed/stage3/car_damages_panel/
data/processed/stage3/car_damages_panel_smoke/
mlflow_local.db
/*.pt


reports/*

# Agent/skills files — not part of project workspace
.agents/
skills-lock.json

# Local environment secrets — never commit
.env
.pytest_cache/

# Archive is tracked for inventory but large binaries inside are not
archive/**/*.pt
archive/**/*.onnx
archive/**/*.engine

# Large archived duplicates from src/Diagnost (exceed pre-commit 5 MB limit)
archive/pre_standardization/notebooks/analyze_train_dataset (1).ipynb
archive/pre_standardization/notebooks/study_stage2_tiled_dataset (1).ipynb
archive/pre_standardization/notebooks/study_stage2_tiled_dataset (1).pdf
archive/pre_standardization/notebooks/Diagnost/
