# Enforce LF line endings for all text files to prevent cross-platform CRLF bugs
* text=auto eol=lf
# Explicitly mark binary files so Git doesn't try to normalize them
*.pt filter=lfs diff=lfs merge=lfs -text
*.png binary
*.jpg binary
*.jpeg binary
*.onnx binary
*.zip binary
